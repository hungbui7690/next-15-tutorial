/*
  Form Submission Status with useFormStatus
  1. components/meals/meals-form-submit
    -> create the button with the disabled attribute
    -> when loading -> disable the button
  2. app/meals/share/page

  ❌ Note: because the slug is set to UNIQUE in db 
    -> error will appear if you try to share the same meal twice
    -> in the real world -> we need to handle this case


\\\\\\\\\\\\\\\\\\\\\\\\\\\\

  Adding Server-Side Input Validation
  3. lib/actions.js


*/
